[build.environment]
  NODE_VERSION = "14"
  PYTHON_VERSION = "3.7"

[build]
  publish = "./dist"
  command = "npx pnpm i --frozen-lockfile --store=node_modules/.pnpm-store && npx pnpm run build"
  functions = "functions"
